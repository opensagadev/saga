# Exact baseline for global text symbols implemented by the current build but
# absent from the original libTTapp.so.
#
# Keep one symbol per line in sorted order. check_symbols.py reports both new
# extras that must be added deliberately and stale entries that must be removed.
# This makes baseline changes reviewable by symbol instead of allowing any
# symbols up to a numeric count.
# Parser table accessor between charconfig.cpp and cc.cpp; keeps the original
# ConfigChar_Keywords table private to its owning translation unit.
_Z22CharConfig_GetKeywordsv
