# Symbols intentionally excluded by this directory's symbol-surface check.
#
# These are file-static (local) helper functions from statically-linked
# third-party libraries that are baked into the original libTTapp.so, but are
# NOT game code and must not be re-implemented as stubs. They are provided by
# the linked static libraries at link time:
#   - zlib implode/explode (make_code, make_leni)
# Vorbis now uses the original C++ linkage and is checked directly.
_ZL8make_leni
_ZL9make_codeiPhPt
_ZL8downheapi
_ZL9count_leni

# Local game helpers whose reconstructed bodies are present, but GCC 4.7
# optimizes away the standalone symbol. MoveAnim_Manage is fully inlined into
# its game-animation callers; the terrain predicate is emitted as the
# equivalent `.part` clone.
_ZL15MoveAnim_ManageP12GameObject_sfii
_ZL35GizObstacle_SatisfyingTerrainChecksP13GIZOBSTACLE_sP12GameObject_s

# Current PlayerCamPos and DrawParaphernalia bodies are inlined into their
# callers at O3. BlockCode is emitted as a constant-propagated clone.
_ZL12PlayerCamPosP12GameObject_sP7nuvec_sS2_
_ZL17DrawParaphernaliaP12GameObject_s

# DrawLightningBolts remains an empty placeholder, eliminated at O3. This is
# an outstanding implementation gap, not a completed or matched function.
_ZL18DrawLightningBoltsP12GameObject_sS0_i

# glibc symbols
sprintf
strstr
rand

# Compiler runtime and third-party libogg symbols supplied under a different
# linkage surface by the reconstructed build.
ogg_stream_flush_fill
